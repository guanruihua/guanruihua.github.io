import { toChartOptions } from '../controller/chart/utils.js'

export const IChart = [
  // {
  //   path: '/chart/getOptions',
  //   post: async (params, req, res) => {
  //     const { toolType, args } = params
  //     return await toChartOptions(toolType, args)
  //   },
  // },
]
